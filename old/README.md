# Backup (Preact + Vite)

This folder is a snapshot of the repository **before** the `~/cv-design` Next.js app was copied to the project root.

It contains the former `src/`, `index.html`, `vite.config.ts`, `package.json`, `public/` assets, and related config files.
